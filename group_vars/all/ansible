ansible_ssh_user: "user"
ansible_ssh_password: "password"

top_dir: "{{ inventory_dir }}"
template_dir: "{{ top_dir }}/templates"
role_dir: "{{ top_dir }}/roles"
ssh_key_dir: "{{ top_dir }}/ssh_keys"
home_dir: "{{ lookup('env', 'HOME') }}"
